rules:
  - target: invalid_target
    pattern: "something"
    type: glob
    action: deny
    severity: block

  - target: command
    pattern: "valid-pattern"
    type: glob
    action: deny
    severity: block
