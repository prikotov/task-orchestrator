# План релиза v0.1.19

## Метаданные

- Тег релиза: `v0.1.19`
- Линия релиза: `release/0.1`
- Исходная ветка: `release/0.1` (коммит зависимости `ccdf1cb` перед релизным коммитом)
- Ответственный: dp
- Плановая дата публикации: 2026-06-04

## Состав

- Включённые PR: #238
- Основные изменения:
  - обновлён `prikotov/git-workflow`: `v0.1.0` → `v0.2.0`
  - поднято ограничение версии Composer: `^0.1.0` → `^0.2.0`
  - подтверждено, что остальные прямые зависимости `prikotov/*` уже актуальны
- Вне состава релиза:
  - исправление рабочего процесса `Release Phar` для устранения несовпадения путей проверки и сборки

## Риски

- Основные риски: низкие — изменение затрагивает инструменты разработки, поведение во время выполнения приложения не меняется
- Наличие миграций данных: нет
- Риск окна несовместимости: нет
- Обратно несовместимые изменения: нет

## Проверки перед публикацией

- [x] `composer outdated 'prikotov/*' --direct` — все актуальны
- [x] `composer validate --strict` — OK
- [x] `composer audit --locked` — рекомендаций нет
- [x] `make check` — зелёный (`PHPUnit`: 954 теста, 2697 проверок)

## Порядок публикации

1. Библиотека (Packagist) — по тегу `v0.1.19`
2. Phar-артефакт — через рабочий процесс GitHub Actions `Release Phar` по тегу `v0.1.19`

## Проверка после публикации

- Проверить, что GitHub Release `v0.1.19` создан
- Проверить, что рабочий процесс `Release Phar` завершился, а `task-orchestrator.phar` приложен к релизу
- Если Phar-артефакт не приложен, зафиксировать предупреждение: на `v0.1.18` GitHub Release создавался, но артефакт мог не приложиться из-за несовпадения путей проверки и сборки; в `v0.1.19` это не исправлялось
- Проверить проверочную команду: `php task-orchestrator.phar --version`

## Действия при проблеме после релиза

- Стратегия исправления: исправляющий релиз v0.1.20
- Ответственный инженер: dp
