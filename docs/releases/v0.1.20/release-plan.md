# План релиза v0.1.20

## Метаданные

- Тег релиза: `v0.1.20`
- Линия релиза: `release/0.1`
- Исходная ветка: `release/0.1` (исправляющего коммита `1ada354` до коммита релиза)
- Ответственный: dp
- Плановая дата публикации: 2026-06-05

## Состав

- Включённые PR: #241
- Основные изменения:
  - рабочий процесс `Release Phar` использует фактический путь к сгенерированному Phar (`bin/task-orchestrator.phar`) для проверочного запуска и загрузки артефакта
  - устранено несовпадение путей: `box compile --no-config` генерирует `bin/task-orchestrator.phar`, а рабочий процесс ранее искал `task-orchestrator.phar` в корне репозитория
- Вне состава релиза:
  - поведение приложения в рантайме (поведение во время выполнения) не меняется
  - миграции данных не требуются
  - обратно несовместимые изменения отсутствуют

## Риски

- Основные риски: низкие — изменение затрагивает только рабочий процесс выпуска и публикацию Phar-артефакта
- Наличие миграций данных: нет
- Риск окна несовместимости: нет
- Обратно несовместимые изменения: нет

## Проверки перед публикацией

- [x] разбор YAML средством Symfony YAML — OK
- [x] `make check` — зелёный (`PHPUnit`: 954 тестов, 2697 проверок)
- [x] CI `test` — пройден

## Порядок публикации

1. Библиотека (Packagist) — по тегу `v0.1.20`
2. Phar-артефакт — через рабочий процесс GitHub Actions `Release Phar` по тегу `v0.1.20`

## Проверка после публикации

- Проверить, что GitHub Release `v0.1.20` создан
- Проверить, что рабочий процесс `Release Phar` завершился успешно для тега `v0.1.20`
- Проверить, что сгенерированный Phar `bin/task-orchestrator.phar` создан в рабочей области процесса и загружен в GitHub Release как релизный `.phar`-артефакт
- Выполнить проверочную команду: `php bin/task-orchestrator.phar --version` в рабочей области процесса; после скачивания релизного артефакта — `php task-orchestrator.phar --version` или команду с фактическим именем `.phar` файла

## Действия при проблеме после релиза

- Стратегия исправления: исправляющий релиз v0.1.21
- Ответственный инженер: dp
