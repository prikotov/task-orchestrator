# План релиза v0.9.1

## Метаданные

- Тег релиза: `v0.9.1`.
- Тип повышения версии: `patch`.
- Основание: только уточнение документации навыков `epic-via-subagents` и `task-via-subagents`; код, упаковка, конфигурация и зависимости не изменялись.
- Источник релиза: `main`; локальная политика `docs/releases/RELEASE-POLICY.md` имеет приоритет над общим руководством и не предусматривает `release/0.9`.
- Зафиксированная база состава до подготовки: `eb98f84e182475bf30f72a47e757ab8eca3d3974`.
- Последний опубликованный тег: `v0.9.0`.
- Рабочая ветка подготовки: `task/release-v0-9-1`.
- Запрос на слияние подготовки: [#420](https://github.com/prikotov/task-orchestrator/pull/420); релиз, тег и слияние этим планом не выполняются.
- Ответственный: владелец проекта.
- Плановая дата публикации: 2026-09-25.

## Состав и границы

Сравнение: [`v0.9.0...v0.9.1`](https://github.com/prikotov/task-orchestrator/compare/v0.9.0...v0.9.1).

В релиз входят только:

- [#419](https://github.com/prikotov/task-orchestrator/pull/419), коммит слияния `eb98f84e182475bf30f72a47e757ab8eca3d3974`: правила таймаутов и черновых коммитов конвейера сабагентов.
- Подготовительный запрос на слияние с `CHANGELOG.md`, этим планом и задачей `TASK-release-v0-9-1-preparation`.

Содержание изменений:

- Шаг 4 (таймаут) `epic-via-subagents`: оркестратор не коммитит чужую незавершённую работу сабагента; незакоммиченный diff — точка продолжения; soft-таймаут при перезапуске увеличивается относительно предыдущего запуска.
- Таблица таймаутов `task-via-subagents`: то же правило увеличения при перезапуске.
- Шаг 8 `epic-via-subagents`: убрано дублирование правил подтверждения слияния проекта-потребителя. Калибровки под конкретный проект в общие навыки не входят — они закреплены в роли Лида проекта-потребителя.

## Совместимость и установка

- CLI- и PHP-контракты, конфигурация, PHAR-состав и зависимости не изменялись; миграции и новые переменные окружения не нужны.
- Навыки поставляются в Composer-пакете как раньше; ручная установка не нужна.

## Проверки перед тегом

Выполнять после слияния подготовительного запроса на точной принятой вершине `main`, а не на этой ветке:

```bash
git switch main
git pull --ff-only origin main
git status --short
git rev-parse HEAD
composer validate --strict
make check
make phar-e2e
```

На момент подготовки не выполнены: `make check` и `make phar-e2e`; они не заменяются документными проверками и должны быть повторены на принятом `main`.

## Тег, публикация и проверки после тега

1. После зелёных проверок и явного разрешения пользователя создать неизменяемый аннотированный тег на проверенной вершине `main`:

   ```bash
   git tag -a v0.9.1 -m "chore(release): v0.9.1"
   ```

2. Отправить только этот тег по утверждённому HTTPS-рецепту GitHub App. GitHub Release создаётся workflow `Release Phar` автоматически.
3. Дождаться успешного `Release Phar`; проверить опубликованный asset:

   ```bash
   gh run list --workflow 'Release Phar' --event push --limit 1
   gh release view v0.9.1 --json tagName,assets
   ```

4. Не изменять и не перемещать тег. При ошибке — отдельный patch из `main` через Pull Request.

## Риски

- Между подготовкой и тегированием `main` может измениться; состав, проверки и тег повторно привязываются к фактически принятой вершине.
- Изменение docs-only: на код, PHAR и зависимости не влияет; риски совместимости отсутствуют.

## Действия при проблеме

- Откат не используется; опубликованный тег не изменяется; исправление выпускается отдельной patch-версией.
