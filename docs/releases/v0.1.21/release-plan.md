# План релиза v0.1.21

## Метаданные

- Тег релиза: `v0.1.21`
- Линия релиза: `release/0.1`
- Исходная ветка: `release/0.1` (исправляющий коммит `e987ebd` до коммита релиза)
- Ответственный: dp
- Плановая дата deploy: 2026-06-05

## Состав

- Предыдущий тег рабочей версии: `v0.1.20`
- Включённые PR: #243
- Основные изменения:
  - workflow `Release Phar` использует `box compile --config=box.json.dist` вместо сборки без конфигурации
  - `box.json.dist` теперь учитывается при сборке Phar, поэтому в артефакт попадают `config/services.yaml`, `config/console_services.yaml`, `config/chains.yaml` и другие файлы из конфигурации Box
  - устранена первопричина релиза `v0.1.20`: правильный путь Phar уже использовался, но сборка через `--no-config` игнорировала `box.json.dist`; из-за отсутствия `config/services.yaml` smoke падал с `FileLocatorFileNotFoundException`
- Вне состава релиза:
  - поведение приложения при выполнении не меняется
  - миграции данных не требуются
  - обратно несовместимые изменения отсутствуют

## Риски

- Основные риски: низкие — изменение затрагивает только workflow релиза и состав Phar-артефакта
- Наличие миграций данных: нет
- Риск окна несовместимости: нет
- Обратно несовместимых изменений: нет

## Проверки перед deploy

- [x] разбор YAML средствами Symfony YAML — пройден
- [x] `--no-config` отсутствует в workflow
- [x] `box compile --config=box.json.dist` согласован с результатом `task-orchestrator.phar`
- [x] `make check` — пройден (`PHPUnit`: 954 теста, 2697 проверок)
- [x] CI `test` — пройдена

## Порядок deploy

1. Библиотека (Packagist) — по тегу `v0.1.21`
2. Phar-артефакт — через GitHub Actions workflow `Release Phar` по тегу `v0.1.21`

## Post-check

- Проверить, что GitHub Release `v0.1.21` создан
- Проверить, что workflow `Release Phar` завершился успешно для тега `v0.1.21`
- Проверить, что GitHub Release `v0.1.21` содержит артефакт `task-orchestrator.phar`
- Скачать asset `task-orchestrator.phar` и проверить проверочную команду: `php task-orchestrator.phar --version`

## Действия при проблеме после релиза

- Стратегия исправления: исправляющий релиз v0.1.22
- Ответственный инженер: dp
