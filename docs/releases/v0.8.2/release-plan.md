# План релиза v0.8.2

## Метаданные

- Тег релиза: `v0.8.2`
- Тип повышения версии: `patch`
- Обоснование выбранной версии: docs-only изменение (SKILL.md скилла поставляется пакетом, публичный runtime-контракт не меняется) — стабилизация линии `0.8.x`
- База релиза: актуальная `main` (`0518521`) по `docs/releases/RELEASE-POLICY.md`
- Рабочая ветка подготовки: `task/release-v0-8-2`
- PR подготовки релиза: см. поле `pr` задачи `TASK-release-v0-8-2-preparation`
- Последний опубликованный тег: `v0.8.1`
- Ответственный: Тимлид Алекс (pi)
- Плановая дата публикации: 2026-09-17

## Состав

- [#398](https://github.com/prikotov/task-orchestrator/pull/398) — плейсхолдер-форма команд в `run-subagent/SKILL.md` (10 блоков) + закрытие кандидата в реестре ретро.

## Совместимость и известные ограничения

- Код и тесты не меняются; CLI-контракт `0.8.x` сохранён.
- Оба скилла со скриптами (`become-role`, `run-subagent`) теперь на плейсхолдер-форме.
- Миграции и новые переменные окружения отсутствуют.

## Порядок публикации

1. Слить подготовительный PR в `main` после успешного CI, ревью и явного подтверждения merge.
2. Создать и отправить тег `v0.8.2` на коммит слияния.
3. Дождаться workflow `Release Phar` (GitHub Release + `task-orchestrator.phar`).
4. Проверить опубликованный PHAR и описание выпуска.

## Проверки перед публикацией

- `composer validate --strict`
- CI подготовительного PR успешен (docs-only: `make check` пропущен по исключению)

## Проверки после публикации

- GitHub Release `v0.8.2` содержит непустой `task-orchestrator.phar`.
- `php task-orchestrator.phar --version` сообщает `0.8.2`.

## Действия при проблеме после релиза

- Откат и изменение опубликованного тега не используются; исправление — patch-релиз `v0.8.3`.
