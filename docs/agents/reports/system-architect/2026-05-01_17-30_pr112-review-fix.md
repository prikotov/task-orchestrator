# Исправление замечания ревьювера: SecurityPolicyCheckInterface → актуальные интерфейсы

**Роль:** Архитектор Гэндальф
**Дата:** 2026-05-01
**Объект:** `docs/releases/security-policy-cross-cutting-analysis.md`, секция 3.2
**Задача:** [TASK-docs-security-policy-analysis](../../../todo/TASK-docs-security-policy-analysis.todo.md), PR #112

---

## Замечание ревьювера

В секции 3.2 (Вердикт по G4) упоминался несуществующий интерфейс `SecurityPolicyCheckInterface`, тогда как в секции 4.3 чётко определены `ChainSecurityPolicyInterface` и `ExecPolicyInterface`. Фраза «достаточно одного интерфейса» также не согласовывалась с наличием двух интерфейсов в 4.3.

## Исправление

**Было:**
> Shared Kernel не разрастается — достаточно одного интерфейса `SecurityPolicyCheckInterface`, применяемого в разных точках.

**Стало:**
> Shared Kernel не разрастается — достаточно двух интерфейсов (`ChainSecurityPolicyInterface` + `ExecPolicyInterface`), применяемых в разных точках (см. секцию 4.3).

## Изменённые файлы

- `docs/releases/security-policy-cross-cutting-analysis.md` — 1 строка заменена

## Коммит

`07b982d` — `fix(docs): replace SecurityPolicyCheckInterface with actual interface names in section 3.2`

Pushed to `task/docs-security-policy-analysis` → PR #112.
