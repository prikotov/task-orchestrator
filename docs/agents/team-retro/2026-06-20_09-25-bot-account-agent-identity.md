# Ретроспектива: bot-account — GitHub App `prikotov-agent` identity для AI-агентов

**Дата:** 2026-06-20
**Задача:** `TASK-chore-bot-account-for-agent` (1 задача, 1 PR #275, 5 коммитов)
**Сложность:** C2
**Что делали:** Разделение идентичности AI-агента и владельца репо. Раньше `gh` работал от `prikotov` → агент не мог approve свой PR → branch protection обходилась через `--admin`. Решение: GitHub App `prikotov-agent` (одна идентичность на все ~36 репо и всех агентов pi/codex/opencode, короткоживущие installation tokens). Реализован PHP-скрипт `bin/agent-token` (JWT RS256 через `ext-openssl`, без зависимостей, кеш) + гайд `docs/guide/agent-identity.md`.

**Роли:** Тимлид Алекс, Бэкендер Тони (реализация), Тех. писатель Гермиона (документация), Ревьювер Бэка Пуаро (code review).

## Оценка ролей

- **Тимлид Алекс** (`docs/agents/roles/team/team_lead_alex.ru.md`)
  - **Плюсы:**
    - Рефрейминг постановки. Исходная задача в backlog содержала неверную модель («бот на агента» с вариантами A/B). За 3 раунда диалога с пользователем докопался до сути: идентичность нужна одна на всех агентов, и при ~36 репо GitHub App выгоднее бот-аккаунта. Это именно работа тимлида — не выполнить как написано, а выяснить реальную потребность.
    - **⚠️ Улучшение отработало vs прошлые ретро (2026-04-19, 2026-04-24, 2026-05-13):** Тимлид НЕ делал задачу сам. Весь код — Бэкендер Тони, вся документация — Гермиона, review — Пуаро. Тимлид только оркестрировал + контролировал git.
    - Правильно митигировал падения сабагентов: после каждого stall/timeout проверял `git status`/`git diff`, убеждался что код внесён, и продолжал с того места (а не перезапускал с нуля).
  - **Минусы:**
    - Не предусмотрел gitleaks-блокировку тестовыми PEM-фикстурами — потребовалась отдельная итерация на Тони (вынос synthetic в fixtures + allowlist). Предсказуемо, можно было заложить в изначальный промпт.
    - Не знал о «генерируемых vs отслеживаемых каталогах docs/» — Гермиона первой попыталась положить гайд в `docs/git-workflow/` (в `.gitignore`), поймал только на верификации через `git check-ignore`.
  - **Предложения по улучшению:** см. раздел «Предложения» (чеклист перед commit; документировать правило про каталоги docs/).

- **Бэкендер Тони** (`docs/agents/roles/team/backend_developer_tony.ru.md`) — реализация
  - **Плюсы:**
    - Качественная реализация: JWT RS256 через `ext-openssl` без сторонних зависимостей, кеш `installation_id` + токен (TTL), fail-fast без утечки секретов, тестируемая архитектура (чистые функции в `bin/lib/`).
    - **Зрелый self-review:** сам нашёл 8 Change Request'ов в собственной работе (CR#1 безопасность: PEM-фильтр в `cleanup_openssl_error`; CR#2 Psalm не сканировал `bin/`; CR#3+#5 тесты кеша не тестировали TTL-логику; CR#8/9 надёжность). Это правильное отношение — не «всё ок», а критичный разбор.
    - Все 3 итерации доработок (self-review CR, code review CR, non-blocking N) закрыты корректно, тесты зелёные.
  - **Минусы:**
    - Self-review — та же роль, что и реализация. Тони не увидел то, что нашёл независимый Пуаро (CR-1 падение на повреждённом кеше, CR-2 кеш не снимал сетевой вызов, N-2 нарушение exit-контракта). Это ограничение self-review, не вина роли — именно поэтому обязателен независимый review.
    - Тест кеша (CR#3+#5 в self-review) изначально «имитировал деятельность»: парсил JSON вручную, не вызывая реальную `read_cache()`. Пуаро поймал в T-1/N-1 то же и для redact-функции.
  - **Предложения по улучшению:** без предложений для файла роли — поведение роли в норме, ограничение структурное (self-review vs независимый review).

- **Тех. писатель Гермиона** (`docs/agents/roles/team/technical_writer_hermione.ru.md`) — документация
  - **Плюсы:**
    - Подробный гайд (9 разделов: концепция «Идентичность ≠ Токен ≠ Доступ», пошаговая настройка, permissions, PEM, troubleshooting, чек-лист DoD) с рабочими ссылками.
    - **Сама подняла критический блокер:** обнаружила, что `docs/git-workflow/` целиком в `.gitignore` (генерируется init-скриптом), и корректно эскалировала тимлиду, а не молча сделала `git add -f` или не тронула `.gitignore`.
    - Честная корректировка размещения после CR: перенесла гайд `git-workflow → guide`, откатала правки в генерируемых файлах, сверила ссылки (`make md-links` → 348 файлов валидны), исправила битые ссылки-источники (CR-4, curl-верификация 200).
  - **Минусы:**
    - Не знала о конвенции «генерируемые каталоги docs/» до того, как положила туда первый вариант гайда. Потеряна часть итерации.
  - **Предложения по улучшению:** см. раздел «Предложения» (документировать правило про каталоги docs/ — это решит минус Гермионы системно).

- **Ревьювер Бэка Пуаро** (`docs/agents/roles/team/code_reviewer_backend_puaro.ru.md`) — code review
  - **Плюсы:**
    - Точный, фактологичный review. 1-й проход: 4 блокера + 4 рекомендации + 3 тест-замечания, каждое с файлом/строкой/severity и воспроизведением.
    - Нашёл то, что исполнитель (Тони) пропустил в self-review: CR-1 (`json_decode` без try/catch → краш на повреждённом кеше), CR-2 (кеш не снимал сетевой вызов), N-2 (JsonException → exit 255 вместо 1, нарушение контракта). **Контраст подходов сработал как задумано.**
    - curl-проверил ссылки сам, а не доверил. Отдал APPROVAL только после реального закрытия всех замечаний, без формализма.
  - **Минусы:** без замечаний.
  - **Предложения по улучшению:** без предложений — эталонная работа ревьювера.

## Что прошло хорошо

- **Тимлид делегировал, а не делал сам.** Это закрыло повтор «Тимлид выполняет задачи сам» (ретро 2026-04-19, 2026-04-24, 2026-05-13). Весь код/документация/review — через сабагентов разных ролей.
- **Обязательный цикл self-review → code review отработал полностью.** Тони нашёл 8 CR в self-review, Пуаро нашёл 4 блокера + рекомендации в независимом review. Code review дал APPROVAL только после закрытия всех замечаний. Ни один шаг не пропущен.
- **Рефрейминг постановки.** Тимлид не стал выполнять неверную постановку «бот на агента», а выяснил реальную потребность за 3 раунда диалога. Результат — GitHub App (легитимнее, безопаснее, масштабируемее), а не второй аккаунт.
- **Многослойная безопасность.** PEM chmod 0600, redact PEM-фрагментов из ошибок OpenSSL, charset-assert токена перед eval, fail-fast на HTTP/parse-ошибках, chmod 0600 на cache-файлы, кеш в `var/` (gitignored). Каждый слой добавлен итеративно через review.
- **Проверки зелёные:** 1125 тестов / 3097 assertions, Psalm 0 errors, gitleaks чист, CI (`test` + `phar-smoke`) pass.
- **Корректная работа с git mv gotcha:** правки метаданных задачи, не перенесённые `git mv`, пойманы на `git status` и закоммичены отдельным коммитом-поправкой (а не потеряны).

## Проблемы

- ⚠️ **Повтор «неполные метаданные задач после сабагента»** (ретро 2026-04-22, 2026-04-27, 2026-06-15 — **4-я ретро подряд**). В новом виде: `git mv todo/... → done/...` не перенёс unstaged-правки метаданных (status=done, pr=#275, чек-листы, history), которые я внёс ПОСЛЕ переноса. Потребовался отдельный коммит-поправка. Предложение «добавить чеклист перед commit в SKILL» звучало в трёх прошлых ретро — **не внедрено**, повторилось снова.

- 🟡 **2 сабагента упали (stall + soft-timeout).** Тони (доработка по CR) завис по `stall` (`runner_failed_after_agent_end`, exit 143); Тони (последняя итерация N-1..N-4) ушёл по `soft-timeout` 800с. В обоих случаях код был внесён ДО падения — я это проверял и продолжал. Частичный повтор «таймауты сабагентов» (ретро 2026-04-22), но митигирован правильно.

- 🟡 **Конвенция «генерируемые vs отслеживаемые каталоги docs/» нигде явно не зафиксирована.** Гермиона не знала, что `docs/git-workflow/`, `docs/conventions/`, `docs/todo-md/` генерируются init-скриптом и в `docs/.gitignore` — попыталась положить гайд туда первой итерацией. Тимлид поймал на `git check-ignore`. Потеряна часть итерации. Тема `git mv`/`gitignore` всплывала в ретро 2026-04-20, 2026-05-13 — частичный повтор.

- 🟢 **Pre-commit gitleaks заблокировал тестовые PEM-фикстуры.** Стандартная ситуация для throwaway RSA-ключей в тестах, но потребовала отдельной итерации (вынос synthetic-фрагмента в `fixtures/` + расширение `allowlist`). Предсказуемо, можно было заложить в изначальный промпт Тони.

- 🟢 **Постановка в backlog изначально уводила не туда.** Исходная формулировка («бот на агента», варианты A/B) базировалась на неверной модели. Потребовался рефрейминг. Корень: задачи в backlog не проходят валидацию модели перед переносом в `todo/`.

- 🔴 **Грязный хак в Configuration.php пропущен на design-review и self-review/code-review.** После перехода на DDD-модуль контракт Архитектора Гэндальфа заложил конфиг модуля GitIdentity (13 параметров) ВНУТРЬ `src/DependencyInjection/Configuration.php` бандла + дублирующий `$map` в Extension + 12 параметров-инъекций в `LoadGitIdentityConfigService`. Это нарушение инкапсуляции модуля и аномалия (остальные 4 модуля не имеют секций в Configuration.php). Тимлид пропустил на design-review (контракт «выглядел ок»), Левша точно следовала контракту, self-review и code-review Пуаро хвалили «Configuration корректна». Поймано только на прямую критику пользователя («выглядит как грязный хак»). Уборка (Вариант A: конфиг → env + дефолты-константы внутри модуля) потребовала отдельной итерации. Корень: дизайн-контракт архитектора не сверялся с паттернами существующих модулей.

## Предложения для улучшения

- [ ] **🔴 Внедрить чеклист «перед commit/finalize» в SKILL `task-via-subagents`.** — TODO, файл: `docs/agents/skills/task-via-subagents/SKILL.md`. ⚠️ **Повтор из ретро 2026-04-22, 2026-04-27, 2026-06-15 — 4-й раз, ПРИОРИТЕТ №1.** Содержание: после правок метаданных задачи (status, pr, assignee, чек-листы) и её переноса в `done/` — ОБЯЗАТЕЛЬНО выполнить `git status` и убедиться, что: (1) правки попали в коммит (особенно после `git mv` — unstaged-правки не переезжают!), (2) рабочий стад чист, (3) `status: done`, `pr: <url>` заполнены, (4) файл в `done/`, (5) ссылки в эпике обновлены. Без этого — задача считается незавершённой.

- [ ] **🟡 Документировать правило «отслеживаемые vs генерируемые каталоги docs/»** — TODO, файл: комментарий в `docs/.gitignore` + упоминание в роли Тех. писателя `docs/agents/roles/team/technical_writer_hermione.ru.md` (или в `docs/guide/extension.md`). Содержание: `docs/git-workflow/`, `docs/conventions/`, `docs/todo-md/` генерируются init-скриптами и в `.gitignore` — рукописную документацию класть в `docs/guide/`, `docs/adr/`, `docs/agents/`, `docs/research/`. Перед созданием файла в `docs/` — проверить `git check-ignore`. Решит минус Гермионы системно.

- [ ] **🟢 Заложить gitleaks-allowlist для тестовых PEM/секретов в промпт Тони** — внедрено неофициально (`.gitleaks.toml` расширен в этом PR). Формализовать: в роли Бэкендера упомянуть, что throwaway RSA-ключи/синтетика для тестов выносятся в `fixtures/` и добавляются в `[allowlist]` `.gitleaks.toml` с пояснением «не production-секреты».
- [ ] **🔴 Не хардкодить идентичность пользователя/имя продукта в публичном коде; нейтрализация должна покрывать и тесты** — выявила переделка PR #275 (после первоначального merge-кандидата). Корень: 4-кратный промах по теме имён/структур в одном диалоге — (1) неверная модель «бот на агента», (2) хардкод `prikotov-agent` в коде/путях/доке, (3) конфиг-каталог по имени скрипта `agent-token/` вместо продукта, (4) лишняя подпапка `agent-identity/` против YAGNI. Публичный продукт (`prikotov/task-orchestrator`) не должен нести пользовательскую идентичность автора в product-коде/путях/тестах. Правила для ролей: (а) имена/пути namespace'ить под **продукт** (`task-orchestrator`), не под скрипт; (б) дефолты конфига — локально (`secrets/`), не в чужом `~/.config/<username>/`; (в) при нейтрализации хардкода — проверять ВСЕ артефакты, включая тесты (Пуаро поймал `prikotov` в 5 тестах после «нейтрализации»); (г) слушать семантику каталогов, которую задаёт пользователь (`secrets/` = постоянное, `var/` = временное), а не навязывать догматику; (д) YAGNI — не плодить уровни каталогов без текущей потребности. Файл: роли Бэкендера и Тех. писателя, плюс чек-лист нейтрализации в `task-via-subagents/SKILL.md`.
- [ ] **🔴 Архитектурный дизайн-контракт тоже проходит ревью на чистоту, не только реализация.** — выявила уборка грязного хака в Configuration.php (PR #275, поздняя итерация). Тимлид пропустил на этапе design-review, что контракт Архитектора Гэндальфа заложил конфиг модуля GitIdentity (13 параметров) ВНУТРЬ `src/DependencyInjection/Configuration.php` бандла — нарушение инкапсуляции модуля и аномалия (остальные 4 модуля не имеют секций в Configuration.php). Реализатор (Левша) точно следовала контракту, хак перешёл из дизайна в код. Поймано только на прямую критику пользователя («выглядит как грязный хак»), НЕ на self-review/code-review Пуаро (он хвалил «Configuration корректна»). Урок: **дизайн-контракт архитектора — не гарантия чистоты; тимлид обязан сверять архитектурный дизайн с конвенциями и паттернами существующих модулей (как устроены соседи), а не только с постановкой.** Добавить в роль Тимлида / Архитектора: на design-review проверять «как это сделано у соседних модулей?» и «не тащим ли чужой baggages в общий код?». Файл: `docs/agents/roles/team/team_lead_alex.ru.md`, `docs/agents/roles/team/system_architect_gandalf.ru.md`.
- 🟡 **Конфигурация модуля принадлежит модулю (env + дефолты-константы), не конфигу бандла.** — выявила та же уборка хака. Паттерн-правило: bounded context хранит свои настройки (секреты, протокольные дефолты) сам — через env-переменные с дефолтами-константами внутри Infrastructure-сервиса. `Configuration.php` бандла — только для общих параметров (paths, roles_dir). Это согласуется с тем, как проект уже работает (`CODEX_HTTP_PROXY` через env). Зафиксировать в конвенциях/роли Архитектора. Файл: `docs/conventions/` (или ADR) + роль Архитектора.
- [ ] **🔴 КОНВЕНЦИИ ПЕРВИЧНЫ НАД ПРИМЕРАМИ ИЗ КОДА — прописать в AGENTS.md.** — выявила очередная итерация (пользователь: «что написано в конвенциях про конфигурацию? почему конвенции не первичны?»). При уборке хака в Configuration тимлид защищал «как у соседей» (4 модуля тоже в config/services.yaml), а должен был открыть конвенцию `docs/conventions/modules/configuration.md` ПЕРВЫМ — она требует `src/Module/<Name>/Resource/config/services.yaml` + параметры `module.<name>.*`. Корень системный: конвенции открывались постфактум (после критики), а не первым шагом до решения — нарушение AGENTS.md «сопоставь с Конвенциями перед написанием кода». ФИКС: в AGENTS.md (раздел «Терминология») прописано правило «Конвенции первичны — источник истины; примеры из кода — низший приоритет, могут содержать долг; при расхождении правильный код по Конвенции + задача техдолга». ✅ **Внедрено в этом PR.** Плюс: GitIdentity приведён к конвенции (Resource/config/services.yaml + module.git_identity.*); на расхождение 4 остальных модулей заведена TASK-techdebt-modules-configuration-convention (в этом же PR, по решению владельца). Урок-мета для Тимлида: открывать `docs/conventions/` ДО архитектурного решения, не после.

## Метрики

- Задач: 1 (C2, переросла в C3-C4 при переходе на DDD-модуль) | PR: 1 (#275) | коммитов: 9
- Сабагентов запущено: 10 (Гэндальф×1, Левша×3, Гермиона×3, Пуаро×2 + итерации по CR)
- Падений сабагентов: 3 (stall + 2 soft-timeout) — все митигированы проверкой прогресса
- Итераций review-цикла: self-review → code review (CHANGE REQUEST) → доработки → code review (APPROVAL) → переделка под DDD по конвенциям → code review (REQUEST CHANGES: блокер URL + слои) → доработки → code review → уборка хака Configuration
- Проверки (финал): 1214 тестов OK, Psalm 0 errors, deptrac 0 violations, gitleaks чист, CI pass
- Созданных строк: ~+5000 / −2700 (с учётом переделки standalone → DDD-модуль + уборка хака)
- Революций архитектуры в одной задаче: 3 (standalone bin/ → нейтрализация хардкода → DDD-модуль → уборка хака в Configuration). Норма для одной задачи — 0-1.

## Важные комментарии

- ⚠️ **Повтор из ретро 2026-04-22 / 2026-04-27 / 2026-06-15:** «неполные метаданные задач после сабагента» — в новом виде (git mv gotcha). Предложение «чеклист перед commit» звучало 3 раза, не внедрено, повторилось. **Приоритет №1 для внедрения.** Механика на этот раз иная (`git mv` не переносит unstaged-правки), но симптом тот же: «задача в `done/`, а метаданные не обновлены».

- 🟡 **Новый системный паттерн:** «генерируемые каталоги docs/». `docs/git-workflow/`, `docs/conventions/`, `docs/todo-md/` создаются init-скриптами и не трекаются git — рукописную документацию туда класть нельзя (перезатрётся при перегенерации). Это неочевидно и нигде явно не задокументировано для агентов. Частичный повтор темы gitignore из ретро 2026-04-20, 2026-05-13.

- ✅ **Улучшение подтверждено:** «Тимлид выполняет задачи сам» (ретро 2026-04-19, 2026-04-24, 2026-05-13) — в этой задаче НЕ повторилось. Вся реализация/документация/review делегированы сабагентам разных ролей. Тимлид только оркестрировал и контролировал git.

- 📌 **Урок по self-review:** исполнитель не видит собственные слепые зоны. Тони в self-review пропустил то, что нашёл независимый Пуаро (CR-1, CR-2, N-2). Подтверждает ценность обязательного независимого review с другой ролью — отказываться от него нельзя, даже когда self-review «зрелый».

- 📌 **Модель эксплуатации задачи:** значительная часть DoD этой задачи — физически ручные действия в GitHub UI (создание App, выпуск PEM, проверка тестовым PR), доступные только владельцу. Агент выполнил техническую часть (скрипт + гайд + чек-лист), задача в `done` по техническим критериям, но бизнес-цель закрывается пользователем отдельно. Стоит учитывать при постановке подобных ops-задач: явно разделять «что делает агент» и «что делает человек».

- 🔴 **Урок (свежий, 2026-06-21): «признания на словах без записи в ретро — ничего не стоят».** На фоне многократных промахов (хардкод идентичности, грязный хак в Configuration) тимлид регулярно «признавал» огрехи в диалоге, но НЕ сразу фиксировал их в этом ретро-файле. Пользователь прямо спросил «ты фиксируешь свои признания и огрехи в ретро?» — и это вскрыло пробел: свежий косяк с Configuration.php был «признан», но не записан. Ретро обновляется в момент признания, а не «потом» — иначе повторится. Мета-урок для роли Тимлида: **ретроспектива — живой документ, обновляется при каждом вскрытом огрехе, а не один раз в конце.**

- 🔴 **Урок (свежий, 2026-06-21): «не преувеличивай сложность, чтобы отговориться от работы».** На предложение владельца «Путь A — полноценный Kernel/ModuleSystem» тимлид ответил «огромная переработка, не этого PR». Владелец поправил: «насчёт 'огромная', ты преувеличиваешь». Проверка фактами: ModuleSystem TasK = 300 строк, переносимое ядро для CLI-library = ~90 строк, нулевые новые зависимости. Реальная работа — один заход Левши. Корень: защитная реакция на объём вместо трезвой оценки по факту. Мета-урок для Тимлида: **прежде чем говорить «слишком сложно/объёмно» — оцени по фактам (строки, зависимости, аналоги), не по ощущению.** Преувеличение сложности = отказ от правильного решения.

- 🟡 **PHAR-whitelist — скрытая зависимость от структуры (свежий, 2026-06-21).** ModuleSystem локально работал, но phar-smoke упал: box.json.dist имеет явный whitelist файлов/директорий, и новые артефакты по конвенции (config/modules.php, src/Module/*/Resource/config/services.yaml) в него не попадают. Урок: **любое изменение структуры (новые config/yaml-файлы вне стандартных путей) требует синхронизации с box.json.dist**, иначе Phar-сборка тихо сломается. Зафиксировать в конвенции packaging/PHAR или чек-листе «перед commit». Сейчас whitelist точечный (одна строка на модуль) — при росте числа модулей понадобится glob/finder в box-конфиге (отдельная задача).

- 🟡 **Library→application: 'bundle' vs 'standalone-tool с vendor-binary' — терминологический косяк тимлида (свежий, 2026-06-22).** На постановку владельца «task-orchestrator — приложение, не bundle, делай Kernel как в TasK» тимлид трижды наворотил ложных проблем: (1) «огромная переработка» (преувеличил — реально 5 слоёв, 1-2 захода Левши), (2) «vendor-binary конфликтует с Kernel» (ложь — vendor/bin/* это ОТДЕЛЬНЫЙ ПРОЦЕСС со своим Kernel, как PHPUnit/PHPStan/Psalm, никакого встраивания Kernel-в-Kernel нет), (3) «убрать bin из composer» (неверно — standalone tool с vendor-binary это норма, пример PHPUnit). Владелец поправил примером PHPUnit, и был прав на 100%. Корень: тимлид путал Symfony Bundle (встраиваемый модуль в чужой Kernel, FOSUserBundle) со standalone tool (vendor-binary, отдельный процесс). Мета-урок для Тимлида: **прежде чем возводить архитектурную 'проблему' в ранг блокера — проверь аналоги (PHPUnit/PHPStan) и не путай bundle с standalone-tool.**

- 🟢 **Kernel-переход в одном PR (свежий, 2026-06-22).** Вопреки рекомендации тимлида 'вынести в отдельный Epic' владелец настоял: 'тащить Kernel-переход в этот PR'. Выполнено за 2 захода Левши (слои 1-3 каркас, 4-5 bin/*+уборка Extension) + 3 Phar-фикса тимлида (box.json whitelist, check-requirements:false, Application напрямую вместо контейнерного сервиса). Результат: Symfony Kernel + ModuleKernelTrait + bundles/packages как в конвенциях, Phar работает, 1220 тестов зелёные. Урок: **рекомендация 'раздели на Epic' не всегда верна — иногда единый PR с итеративной проверкой быстрее и связнее, чем распыление по задачам.** Но только если тимлид готов вести многослойную работу с gate-проверками между слоями.

- 🟡 **Конвенции — внешний проект (свежий, 2026-06-22).** `docs/conventions/` в task-orchestrator — gitignored, приходит через composer из `prikotov/coding-standard` (отдельный репозиторий). Правки конвенций — НЕ в task-orchestrator, а в coding-standard (свой workflow, свой PR). В этом PR обнаружено расхождение внутри конвенций: `layers.md` таблица «Описание слоёв» относит «Внешние API» к Integration, а `integration.md`/`infrastructure.md`/`external-service.md` (образец DeepSeekComponent) — к Infrastructure (3 из 4 согласованы: внешний API = Infrastructure). Чинится в репо coding-standard, не здесь. Урок для тимлида: **прежде чем 'чинить конвенцию' локально — проверь, что docs/conventions отслеживается (git ls-files); если gitignored — это внешний проект, правка идёт отдельным PR туда.**

- 🟡 **HTTP-transport: самопальный trait вместо HttpClient по конвенции (свежий, 2026-06-22).** Гэндальф в контракте заложил GitHubHttpTransportTrait через file_get_contents+stream_context «без сторонних», а конвенция `external-service.md` прямо требует Symfony HttpClientInterface + логирование + обёртку исключений + scoped-client. Тимлид пропустил на design-review (контракт архитектора не свёрен с конвенцией). Поймано только критикой владельца. Переделано на GitHubHttpComponent (HttpClientInterface + LoggerInterface + scoped http_client.git_identity + MockHttpClient в тестах) + GitHubHttpException (Infrastructure, не Domain — deptrac-чисто). 7-е проявление корня «конвенции открываются ДО, не после»: теперь охвачена и конвенция external-service, не только modules/configuration/calculator.
