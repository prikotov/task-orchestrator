# Ретроспектива: docs-architecture-sync + team-lead-delegation

**Дата:** 2026-04-19
**Задачи:** `TASK-docs-architecture-integration-sync`, `TASK-docs-team-lead-delegation-subsection`
**PR:** #25 (единый PR для двух задач)

## Что прошло хорошо

- Технический писатель корректно выполнил свою задачу через сабагента — подраздел «Делегирование» написан чисто, все критерии приёмки соблюдены с первого раза
- Архитектурная документация полностью синхронизирована с кодом: grep по несуществующим классам даёт 0 совпадений

## Проблемы

- **Тимлид выполняет задачи сам вместо делегирования.** Первая задача (актуализация 5 doc-файлов) была выполнена Тимлидом напрямую, без создания задачи и без запуска сабагента. Нарушение собственной роли: «Тимлид не пишет код сам».
- **Новая задача взята без завершения предыдущей.** Архитектурная правка не была формализована как задача, не были пройдены этапы (задача → ветка → коммит → PR). Вместо этого Тимлид сразу взялся за вторую задачу (делегирование). В результате — две задачи пришлось объединять в один PR.
- **Забыта ретроспектива.** После завершения работы ретро не было написано — потребовалось напоминание пользователя.

## Корректирующие действия

- [x] Подраздел «Делегирование» добавлен в `team_lead_alex.ru.md` — правило формализовано: Тимлид предлагает задачу, не создаёт сам; не выполняет сам
- [ ] **Проверить на следующих задачах:** соблюдает ли Тимлид новое правило делегирования на практике

## Урок

Процесс существует не для бюрократии, а для самоконтроля. Каждый пропуск (не создал задачу, сделал сам, забыл ретро) — это место, где можно было поймать ошибку раньше. Правило простое: одна задача → формализована → делегирована → завершена → следующая.
