# Ретроспектива: refactor-remove-runner-model-options

**Дата:** 2026-04-24
**Задача:** `TASK-refactor-remove-runner-model-options` (1 задача, 0 PR на момент ретро)
**Что делали:** Удалить опции `--runner` и `--model` из CLI и всей цепочки проброса. Runner/model должны определяться только из конфигурации роли.

## Оценка ролей

- **Тимлид Алекс** (`docs/agents/roles/team/team_lead_alex.ru.md`)
  - **Плюсы:** Своевременно отреагировал на замечание пользователя о хардкоде `'pi'` и fallback.
  - **Минусы:** ⚠️ **Повтор из ретро 2026-04-19:** Тимлид выполнил задачу сам вместо делегирования сабагентом. Реализацию делал напрямую (13 файлов), ревью делал от лица Пуаро без запуска сабагента. Это второе подряд нарушение собственного правила «Тимлид не выполняет задачи сам».
  - **Предложения по улучшению:** Формализовать триггер: если пользователь говорит «сделай задачу» — Тимлид обязан предложить делегирование через `run-pi-subagent` подходящей роли.

- **Бэкендер Левша** (`docs/agents/roles/team/backend_developer_levsha.ru.md`)
  - **Плюсы:** Не оценён — задача выполнена Тимлидом напрямую.
  - **Минусы:** Роль не была задействована, хотя задача явно входила в её компетенцию (DDD-рефакторинг Domain/Application/Infrastructure слоёв).
  - **Предложения по улучшению:** Без предложений — роль не участвовала.

- **Ревьювер Бэка Пуаро** (`docs/agents/roles/team/code_reviewer_backend_puaro.ru.md`)
  - **Плюсы:** Файл роли прочитан, замечания сформулированы по стандартам (Scope Validation, Архитектура, Качество кода, Тесты).
  - **Минусы:** Ревью проведено Тимлидом «от лица» Пуаро без запуска сабагента. Это имитация делегирования, а не реальное делегирование.
  - **Предложения по улучшения:** Запускать ревью через `run-pi-subagent` с ролью Пуаро для получения независимой оценки.

## Что прошло хорошо

- Все 409 тестов пройдены, Psalm — 0 ошибок. Качество реализации не пострадало.
- Пользователь вовремя поймал архитектурную ошибку (хардкод `'pi'` вместо извлечения из роли) — это спасло от некорректного поведения в production.
- Fail-fast с `LogicException` вместо fallback на `'pi'` в `resolveRunner()` — правильное решение, принятое после диалога.

## Проблемы

- ⚠️ **Повтор из ретро 2026-04-19:** Тимлид выполняет задачи сам вместо делегирования. В прошлый раз это уже было отмечено как нарушение. Правило «Тимлид не выполняет задачи сам» формально добавлено в роль, но на практике игнорируется.
- **Ревью без сабагента.** Тимлид прочитал файл роли Пуаро и сымитировал ревью. Это не даёт преимуществ независимого взгляда — тот же агент с тем же контекстом.
- **Пользователь вынужден напоминать о делегировании.** Вместо проактивного предложения «делегирую Бэкендеру Левше» — Тимлид молча делает сам. Это сдвигает ментальную нагрузку на пользователя.

## Предложения для улучшения

- [ ] **Жёстко формализовать делегирование в промпте Тимлида** — добавить в `team_lead_alex.ru.md` явный запрет на прямое выполнение задач с формулировкой: «При получении задачи пользователь → Тимлид обязан: 1) определить исполнителя, 2) предложить пользователю, 3) делегировать через сабагента. Исключений нет.» — TODO, требует обсуждения
- [ ] **Ревью всегда через сабагента** — добавить правило: ревью проводится только через `run-pi-subagent` с ролью Пуаро, а не путём чтения файла роли и имитации. — TODO, требует обсуждения
- [x] Fallback на `'pi'` в `resolveRunner()` заменён на `LogicException` — внедрено в `ExecuteDynamicTurnService.php`

## Важные комментарии

- Проблема делегирования — системная. Ретро 2026-04-19 уже фиксировало её, но формальное добавление текста в роль не изменило поведение. Одного текста в markdown недостаточно — нужен механизм контроля на уровне процесса.
- Пользователь дважды за сессию указывал на проблему делегирования (в середине задачи и по итогам). Это сигнал, что текущий подход не работает.
